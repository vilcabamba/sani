<ol class="breadcrumb">
  <li class="active">Negocios</li>
</ol>

<span class="pull-right">
  <%= link_to new_business_path, :class => "btn btn-xs btn-default" do %>
    <span class="glyphicon glyphicon-plus"></span>
  <% end %>
</span>

<div class="models">
  <% @businesses.each do |business| %>
    <%= render business %>
  <% end %>
</div>
